# Raw data

The original dataset is downloaded from Kaggle.

The raw CSV file is not stored in the repository because of size limitations.

To reproduce the processed dataset run:

python src/create_dataset.py
