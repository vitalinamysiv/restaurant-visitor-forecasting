# Restaurant Visitor Forecasting

Прогнозирование количества посетителей ресторанных точек на 7 дней вперёд.

---

## Описание проекта

Проект прогнозирует количество посетителей ресторанных точек на **7 дней вперёд**.
Модель может использоваться для планирования загрузки точек, оптимизации персонала и закупок.

- **Целевая переменная:** `guests` — количество посетителей ресторана за день.
- **Дополнительный бизнес-показатель:** `revenue` — дневная выручка.

**Цель проекта** — построить модель прогнозирования спроса, которая может использоваться для планирования загрузки точек и оптимизации ресурсов.

---

## Ключевые решения 

1. **Обработка пропусков.** В датасете Rossmann нет воскресений (магазины закрыты). Поэтому лаги и rolling-средние считаются по календарным дням, а не по количеству строк, а `NaN` в rolling-признаках заполняются медианой по последним 60 дням.
2. **Признаки.** Использованы календарные признаки (`day_of_week`, `month`, `is_weekend`), лаги (`lag_1`, `lag_7`, `lag_14`) и скользящие средние (`rolling_mean_7`, `rolling_mean_28`). Все признаки рассчитываются только на прошлых данных — без утечки будущего.
3. **Валидация.** Временное разделение (train: 2013-01-29 — 2015-06-19, valid: 2015-06-20 — 2015-07-31) вместо случайного — так мы эмулируем реальный сценарий прогноза на будущее.
4. **Модель.** CatBoost выбран как основная модель, потому что он хорошо работает с табличными данными, устойчив к выбросам и улавливает нелинейные зависимости. Ridge и наивный baseline использованы для сравнения.
5. **Инференс.** В `predict.py` используется рекурсивный прогноз: каждый следующий день опирается на предсказание предыдущего. Признаки приводятся к `MODEL_FEATURES` — CatBoost строго проверяет порядок колонок.

---

## Датасет

Используется датасет [Rossmann Store Sales](https://www.kaggle.com/competitions/rossmann-store-sales/data).

**Исходные данные содержат:** дату, идентификатор точки, продажи, количество клиентов, информацию о работе точки (открыта/закрыта, промо, праздники).

**Преобразование колонок:**

| Исходная колонка | Колонка в проекте |
|------------------|-------------------|
| `Store`          | `restaurant_id`   |
| `Customers`      | `guests`          |
| `Sales`          | `revenue`         |
| `Date`           | `date`            |

> Исходный датасет **не хранится в репозитории** из-за размера (~38 МБ).
> Скачайте `train.csv` с Kaggle и положите в `data/raw/train.csv`.

### Особенность данных Rossmann

В датасете **отсутствуют воскресенья** — магазины сети закрыты по воскресеньям. Это создаёт пропуски в датах и влияет на:

- расчёт лагов (`lag_7` может быть не «7 дней назад», а «6 дней назад»);
- расчёт скользящих средних (окно `.rolling(28)` «плывёт»);
- проверки «достаточно ли истории» (нельзя проверять `len(history) < 28`).

В пайплайне это обрабатывается:

- проверкой **календарных дней**, а не количества строк;
- заполнением `NaN` в rolling-признаках **медианой по последним 60 дням**;
- сортировкой данных по дате перед расчётом лагов.

---

## Структура проекта

```
restaurant-visitor-forecasting/
├── data/
│   ├── raw/                    # исходный train.csv (не в git)
│   └── processed/              # restaurant_daily.csv
├── models/
│   └── catboost_model.pkl      # обученная модель
├── notebooks/
│   ├── 00_prepare_data.ipynb
│   ├── 01_eda.ipynb
│   └── 02_modeling.ipynb
├── src/
│   ├── create_dataset.py       # подготовка данных
│   ├── features.py             # feature engineering
│   ├── validation.py           # временное разделение
│   └── model.py                # обучение и загрузка модели
├── tests/
│   └── test_features.py
├── predict.py                  # инференс (прогноз на 7 дней)
├── requirements.txt
└── README.md
```

---

## Как запустить 

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Требуется **Python 3.10+**.

### 2. Подготовка данных

Скачайте `train.csv` с [Kaggle](https://www.kaggle.com/competitions/rossmann-store-sales/data)
и положите в `data/raw/train.csv`. Затем:

```bash
python src/create_dataset.py
```

Результат: `data/processed/restaurant_daily.csv`.

### 3. Обучение модели

```bash
python -m src.model
```

Скрипт обучает `CatBoostRegressor` на train-периоде, валидирует на отложенном периоде и сохраняет модель в `models/catboost_model.pkl`.

### 4. Прогноз

```bash
python predict.py --date 2015-07-01 --restaurant 1
```

**Пример вывода:**

```
       date  prediction
 2015-07-01         602
 2015-07-02         599
 2015-07-03         599
 2015-07-04         595
 2015-07-05         595
 2015-07-06         595
 2015-07-07         595
```

### Аргументы `predict.py`

| Аргумент       | Описание                          | По умолчанию                            |
|----------------|-----------------------------------|-----------------------------------------|
| `--date`       | дата начала прогноза (YYYY-MM-DD) | обязательный                            |
| `--restaurant` | ID ресторана                      | обязательный                            |
| `--model`      | путь к модели                     | `models/catboost_model.pkl`             |
| `--data`       | путь к данным                     | `data/processed/restaurant_daily.csv`   |

---

## Подготовка данных 

```bash
python src/create_dataset.py
```

Скрипт выполняет:

1. Загрузку `data/raw/train.csv`.
2. Выбор необходимых колонок.
3. Исключение закрытых точек (`Open == 0`).
4. Переименование колонок (`Store → restaurant_id` и т.д.).
5. Сохранение результата в `data/processed/restaurant_daily.csv`.

**Итоговая структура данных:**

| Колонка         | Описание                  |
|-----------------|---------------------------|
| `date`          | дата наблюдения           |
| `restaurant_id` | идентификатор точки       |
| `guests`        | количество посетителей    |
| `revenue`       | дневная выручка           |

---

## EDA

EDA выполнен в `notebooks/01_eda.ipynb`.

Исследовано:

- динамика количества посетителей во времени;
- недельная сезонность (пик — суббота);
- месячная сезонность;
- распределение целевой переменной;
- выбросы (не удалялись — могут отражать реальные праздники/акции);
- пропуски (воскресенья).

---

## Feature Engineering

Генерация признаков реализована в `src/features.py` (функция `make_features`).

### Календарные признаки
`day_of_week`, `month`, `day_of_month`, `is_weekend`.

### Лаговые признаки
`lag_1`, `lag_7`, `lag_14`.

### Скользящие статистики
`rolling_mean_7`, `rolling_mean_28`.

### Признаки, используемые моделью

```python
MODEL_FEATURES = [
    'restaurant_id', 'revenue', 'day_of_week', 'month', 'day_of_month',
    'is_weekend', 'lag_1', 'lag_7', 'lag_14',
    'rolling_mean_7', 'rolling_mean_28',
]
```

> Все временные признаки рассчитываются **только на основе прошлых данных** (no data leakage).

---

## Валидация

Используется **временное разделение** — случайное перемешивание не применяется.

| Разбиение   | Период                    |
|-------------|---------------------------|
| Train       | 2013-01-29 — 2015-06-19   |
| Validation  | 2015-06-20 — 2015-07-31   |

Такой подход позволяет оценить качество модели на будущем периоде, как в реальной эксплуатации.

### Обоснование выбора метрик

Для оценки качества модели выбраны **MAE** и **MAPE**, а не RMSE или R², по следующим причинам:

- **MAE (Mean Absolute Error)** — средняя абсолютная ошибка в гостях. 
  Измеряется в тех же единицах, что и целевая переменная, поэтому 
  легко интерпретируется бизнесом: «модель ошибается в среднем на 50 гостей в день». 
  В отличие от RMSE, MAE не возводит ошибки в квадрат, поэтому не «раздувает» 
  влияние отдельных выбросов (например, новогодних пиков), которые для нас — 
  легитимные события, а не аномалии.

- **MAPE (Mean Absolute Percentage Error)** — средняя относительная ошибка в %. 
  Позволяет сравнивать качество модели между ресторанами с разным масштабом 
  потока (для точки на 200 гостей и на 2000 гостей ошибка в 50 человек — 
  это разные вещи). Даёт удобную бизнес-метрику: «модель ошибается на 7% 
  от суточного потока».

---

## Модели

| Модель | Описание |
|---|---|
| **Baseline** | сезонный наивный прогноз: значение `guests` за 7 дней до текущей даты |
| **Ridge Regression** | линейная модель на временных признаках |
| **CatBoost** | `CatBoostRegressor` — основная модель; хорошо работает с табличными данными, устойчива к выбросам, улавливает нелинейности |

---

## Результаты моделей

Метрики: **MAE**, **MAPE** на отложенном периоде (2015-06-20 — 2015-07-31).

| Модель             | MAE   | MAPE  |
|--------------------|-------|-------|
| Baseline (lag_7)   | 163.4 | 24.9% |
| Ridge Regression   | 69.7  | 10.3% |
| **CatBoost**       | **49.8** | **7.1%** |

**Улучшение относительно baseline:**

- MAE: **163.4 → 49.8** (−69%);
- MAPE: **24.9% → 7.1%** (−71%).

---

## Что это значит для бизнеса

**MAE = 49.8 гостей** означает, что модель в среднем ошибается примерно на **50 человек в день**. При среднем потоке ресторана ~700 гостей в день это **относительная ошибка около 7%** (MAPE = 7.1%).

**Достаточно ли этого для планирования?**

- **Смены персонала:** ±50 гостей — это примерно ±1 официант в смену. Такой точности достаточно, чтобы планировать базовое расписание, но в пиковые дни (пятница, суббота) стоит закладывать запас.
- **Закупки:** ошибка 7% приемлема для скоропортящихся продуктов — можно планировать ежедневные закупки с небольшим буфером (10–15%).
- **Для сравнения:** baseline (наивный прогноз) ошибается на 163 гостя (25%), что делает его непригодным для планирования. Модель CatBoost снижает ошибку почти в 3.3 раза.

**Вывод:** модель даёт прогноз, достаточный для оперативного планирования смен и закупок на неделю вперёд. Для критичных решений (крупные мероприятия, праздники) рекомендуется комбинировать прогноз с экспертной оценкой менеджера.

---

## Анализ важности признаков

Топ-5 признаков модели:

| Признак            | Интерпретация                    |
|--------------------|----------------------------------|
| `rolling_mean_28`  | долгосрочный уровень спроса      |
| `revenue`          | масштаб активности точки         |
| `lag_1`            | краткосрочная динамика           |
| `rolling_mean_7`   | недельный тренд                  |
| `day_of_week`      | недельная сезонность             |

---

## Сохранение и загрузка модели

- Обученная модель: `models/catboost_model.pkl`
- Загрузка: `src/model.py` → `load_model(path)`
- Сохранение: `src/model.py` → `save_model(model, path)`

---

## Как работает `predict.py`

1. Загружает обработанные данные, фильтрует по `restaurant_id`, сортирует по дате.
2. Проверяет, что до даты прогноза есть **минимум 28 календарных дней** истории.
3. Загружает модель.
4. **Рекурсивный цикл на 7 дней:**
   - создаёт строку для будущего дня (`guests = NaN`);
   - добавляет её к историческим данным;
   - вызывает `make_features()`;
   - заполняет `NaN` медианой по последним 60 дням;
   - приводит порядок признаков к `MODEL_FEATURES`;
   - получает прогноз;
   - добавляет прогноз как «реальность» для следующего шага.
5. Возвращает DataFrame с датами и прогнозами.

---

## Тесты

```bash
python -m pytest tests/ -v
```

`tests/test_features.py` проверяет:

- отсутствие data leakage;
- корректность rolling-средних;
- наличие всех признаков из `MODEL_FEATURES`.

---

## Ограничения и допущения

1. **Закрытые дни исключены** (`Open == 0`).
2. **Будущая выручка неизвестна** — используется последнее доступное значение `revenue`.
3. **Затухание прогноза.** На 4–7 день прогнозы сходятся к среднему уровню ресторана, так как модель не получает новых реальных данных.
4. **Пропущенные воскресенья** заполняются медианой.
5. **Горизонт 7 дней** — компромисс между точностью и практической ценностью.

---

## Что бы я доделала при наличии времени

- Отдельная модель для прогнозирования `revenue`.
- Добавление внешних факторов: погода, локальные праздники, акции конкурентов.
- Прямое (direct) прогнозирование вместо рекурсивного — снизит затухание.
- Расширение набора признаков: `lag_365` (годовая сезонность), Fourier-признаки.
- Ансамблирование CatBoost + Ridge + LightGBM.
- Мониторинг дрифта данных в production.
- CI/CD: автоматический запуск тестов при каждом коммите.


